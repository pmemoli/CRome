C compiler written in Rust based on Sandler Nora's book "Writing a C Compiler". 

Very much a WIP.

TODO:

- Implement file cleanup on error for the temp files of the compiler driver.
- Handle --lex, --parse, and --codegen flags.
